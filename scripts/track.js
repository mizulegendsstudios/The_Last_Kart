export class Track {
  constructor(shape) {
    this.shape = shape; // 'X', 'O', 'triangle'
    this.ramps = true;
    this.crossings = true;
  }

  render() {
    // Dibuja la pista en canvas o DOM
  }
}
